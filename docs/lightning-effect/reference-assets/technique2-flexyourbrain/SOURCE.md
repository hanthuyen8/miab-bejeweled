# Nguồn gốc asset

Tải từ: https://github.com/FlexYourBrain/sample_LightningVFX
(thư mục gốc: `assets/gfx/AR_vfx/boltz/`)

- `b1.PNG` → `b12.PNG`: 12 frame của sprite animation lightning chính (dùng trong kỹ thuật mesh-stretch + render target, xem `../../lightning-effect.md`).
- `small_bolts1.png` → `small_bolts5.png`: biến thể tia sét nhỏ hơn.

## Cảnh báo license

Repo không có file LICENSE (kiểm tra qua GitHub API trả 404). Tên thư mục chứa asset (`AR_vfx`) gợi ý đây có thể là asset pack của bên thứ ba được bundle vào demo, không hẳn do tác giả repo tự vẽ — nguồn gốc thật của các file ảnh không rõ ràng.

**Không dùng trực tiếp các file này trong game.** Chỉ giữ ở đây để tham khảo hình dáng/tỉ lệ/màu sắc khi tự vẽ lại asset phù hợp với game (theo grid 65px, atlas convention của bejeweled — xem `docs/repo-structure.md`).
